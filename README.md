# Memory Card

This is a simple memory card made game made using React and PokeAPI. The use state hook is used for shuffling the cards and the use effect hook is used to get data from pokeapi to then cache in the browsers localstorage
